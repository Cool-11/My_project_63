，你好，我是系统架构师。我正在使用串口触摸屏（类似淘晶驰方案）作为 ESP32-S3 的 UI 交互端。
请你仔细阅读以下三个页面的 UI 控件映射表。表中的 `ID` 是串口通信操控屏幕对象的唯一标识（如按钮点击事件的 ID，或文本框赋值的目标 ID）。
在后续生成 ESP32 的 C++ 界面交互代码时，请严格使用这些 ID 进行串口指令拼接。

---

## 页面 1：入库页面 (Page: Inbound)
**业务逻辑：** 用于新物品的视觉拍照（YOLO识别）、星闪标签匹配与绑定，最后将数据上报云端完成入库建档。

| 控件ID | 控件类型 | 界面显示文本 | 绑定的业务动作 / 数据用途 |
|---|---|---|---|
| `b3` | Button (按钮) | 返回 | 页面跳转：返回主菜单 |
| `b2` | Button (按钮) | 当前状态 | 触发状态自检，更新 `t5` 文本框 |
| `b0` | Button (按钮) | 匹配标签 | 触发 WS63 扫描寻找未绑定的星闪标签 MAC |
| `b1` | Button (按钮) | 按下后启动摄像头 | 启动 ESP32 摄像头，准备 YOLO 识别 |
| `b4` | Button (按钮) | 拍摄Front | 触发拍摄正面照片 |
| `b5` | Button (按钮) | 拍摄Side | 触发拍摄侧面照片 |
| `b6` | Button (按钮) | 拍摄Top | 触发拍摄顶部照片 |
| `b7` | Button (按钮) | 确认入库 | **[核心]** 将提取的标签ID、物品信息打包发送至云端 |
| `t5` | Text (文本框) | Status (大框) | 显示系统运行日志、报警信息、视觉识别结果 |
| `t0` | Text (文本框) | tag_id | 显示分配给该物品的星闪标签 ID / MAC |
| `t3` | Text (文本框) | name | 显示视觉识别出的物品名称 |
| `t1` | Text (文本框) | count | 显示输入的入库数量 |
| `t2` | Text (文本框) | area | 显示分配的仓储区域 |
| `t4` | Text (文本框) | 当前拍摄： | 显示当前处于哪个拍摄视角模式 |
| `c0` | Checkbox(复选框) | 覆写 (`t6`标签) | 勾选表示覆盖原有标签数据 |
| `c1` | Checkbox(复选框) | 更新 (`t7`标签) | 勾选表示更新现有库存 |

---

## 页面 2：出库页面 (Page: Outbound)
**业务逻辑：** 用于提货时的定向寻呼、扣减库存，并通过视觉再次核验出库物品是否匹配。

| 控件ID | 控件类型 | 界面显示文本 | 绑定的业务动作 / 数据用途 |
|---|---|---|---|
| `b3` | Button (按钮) | 返回 | 页面跳转：返回主菜单 |
| `b2` | Button (按钮) | 当前状态 | 触发状态自检 |
| `b0` | Button (按钮) | 匹配标签 | 读取将要出库的物品星闪标签 |
| `b4` | Button (按钮) | 按下后启动摄像头 | 启动摄像头进行出库防错核验 |
| `b5` | Button (按钮) | 拍摄Front | 采集出库物品图像 |
| `b7` | Button (按钮) | 确认出库 | **[核心]** 提交出库请求，云端扣减库存并解绑 |
| `t5` | Text (文本框) | Status (大框) | 显示出库核验日志、扣减状态 |
| `t0` | Text (文本框) | tag_id | 显示出库物标签 ID |
| `t3` | Text (文本框) | name | 显示出库物品名称 |
| `t1` | Text (文本框) | count | 显示当前在库总数量 |
| `t2` | Text (文本框) | area | 显示物品所在区域 |
| `t7` | Text (文本框) | 出库数量 (标识) | 仅作静态 UI 标识符 |
| `t6` | Input (输入框) | num | 用户手动输入需要出库的数量 |

---

## 页面 3：盘点页面 (Page: Inventory)
**业务逻辑：** 触发 WS63 网关进行星闪 SLE 组播大盘点，或通过单播进行定向寻找。

| 控件ID | 控件类型 | 界面显示文本 | 绑定的业务动作 / 数据用途 |
|---|---|---|---|
| `b3` | Button (按钮) | 返回 | 页面跳转：返回主菜单 |
| `b2` | Button (按钮) | 选择区域并盘点 | **[核心]** 触发 WS63 星闪网关进行区域高频组播扫描 |
| `b0` | Button (按钮) | 匹配标签 | 针对特定资产的单播定向寻呼（声光报警） |
| `b1` | Button (按钮) | 按下后启动摄像头 | 启动摄像头定位展柜外部区域二维码 |
| `b4`, `b5`, `b6` | Button (按钮) | 拍摄 Front/Side/Top | 拍摄盘点环境快照 |
| `t5` | Text (文本框) | Status (大框) | 盘点指令下发状态、扫描进度指示 |
| `t0` | Text (文本框) | Info (大框) | 详细显示扫到的标签列表（Diff 差异比对结果） |
| `t4` | Text (文本框) | 当前拍摄： | 拍摄模式指示 |

 页面 4：寻物与列表视图 (Page: Find_List)

你好，我是系统架构师。我正在使用串口触摸屏作为 ESP32-S3 的 UI 交互端。
此页面的核心业务逻辑是：**拉取在库资产列表、本地分页显示、多选勾选资产，并触发星闪网关的定向单播寻物（声光报警）**。

请在后续生成 ESP32  屏幕控制代码时，严格使用以下 `ID` 字典进行指令拼接（如：`t0.txt="指令已下发"`）。

### 1. 全局控制与状态控件
| 控件ID | 控件类型 | 界面显示文本 | 绑定的业务动作 / 数据用途 |
|---|---|---|---|
| `b0` | Button (按钮) | 获取列表 | 触发 ESP32 从本地缓存/网关拉取最新扫描到的资产数据，并刷新下方列表 |
| `t0` | Text (文本框) | Status (大框) | 顶部状态栏：显示系统日志（如：正在寻呼... / 寻呼已停止） |
| `b5` | Button (按钮) | 返回 | 页面跳转：返回主菜单 |

### 2. 核心业务操作控件（寻物闭环）
| 控件ID | 控件类型 | 界面显示文本 | 绑定的业务动作 / 数据用途 |
|---|---|---|---|
| `b1` | Button (按钮) | 寻找 | **[核心]** 遍历当前列表复选框，提取勾选项的 ID，通过 UART 向 WS63 发送 `0x03` (寻物) Hex 指令 |
| `b2` | Button (按钮) | 停止 | 向 WS63 发送 `0x00` (停止寻物) Hex 指令，关闭标签蜂鸣器 |

### 3. 数据列表控件（6行显示区）
*列表数据格式约定：`标签ID -- 物品名称 -- 所在区域 -- 数量` (例如：`0x0001 -- 扳手 -- A -- 50`)*

| 控件ID | 控件类型 | 界面显示文本 | 绑定的业务动作 / 数据用途 |
|---|---|---|---|
| `c1` | Checkbox (复选) | (第一行勾选框) | 标记第一行资产是否被选中用于寻物 |
| `t1` | Text (文本框) | (第一行数据) | 显示当前页第 1 条资产字符串数据 |
| `c2` | Checkbox (复选) | (第二行勾选框) | 标记第二行资产是否被选中 |
| `t2` | Text (文本框) | (第二行数据) | 显示当前页第 2 条资产字符串数据 |
| `c3` | Checkbox (复选) | (第三行勾选框) | 标记第三行资产是否被选中 |
| `t3` | Text (文本框) | (第三行数据) | 显示当前页第 3 条资产字符串数据 |
| `c4` | Checkbox (复选) | (第四行勾选框) | 标记第四行资产是否被选中 |
| `t4` | Text (文本框) | (第四行数据) | 显示当前页第 4 条资产字符串数据 |
| `c5` | Checkbox (复选) | (第五行勾选框) | 标记第五行资产是否被选中 |
| `t5` | Text (文本框) | (第五行数据) | 显示当前页第 5 条资产字符串数据 |
| `c6` | Checkbox (复选) | (第六行勾选框) | 标记第六行资产是否被选中 |
| `t6` | Text (文本框) | (第六行数据) | 显示当前页第 6 条资产字符串数据 |

### 4. 分页控制控件
| 控件ID | 控件类型 | 界面显示文本 | 绑定的业务动作 / 数据用途 |
|---|---|---|---|
| `b3` | Button (按钮) | 上一页 | 触发 ESP32 本地数组指针前移，重新渲染 ni`t1`-`t6` 和 `c1`-`c6` |
| `b4` | Button (按钮) | 下一页 | 触发 ESP32 本地数组指针后移，重新渲染 `t1`-`t6` 和 `c1`-`c6` |
| `t7` | Text (文本框) | 页数 资产总数 | 显示分页状态（例如：`第 1/5 页 总数: 30`） |